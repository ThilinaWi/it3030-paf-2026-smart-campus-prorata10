package com.smartcampus.model.enums;

public enum TicketPriority {

    //  Low priority issues (not urgent, can be handled later)
    LOW,

    //  Medium priority issues (normal importance, should be handled soon)
    MEDIUM,

    //  High priority issues (urgent, needs immediate attention)
    HIGH
}